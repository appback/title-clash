# Tournament Voting System Design

## Overview

TitleClash는 3개의 독립 콘텐츠를 제공한다.
각 콘텐츠는 고유한 게임 시작 페이지, 토너먼트 투표, 결과 페이지를 갖는다.

---

## 3개 콘텐츠

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  콘텐츠 1: Title Battle          AI 제목 vs AI 제목          │
│  (동일 이미지)                   + 선택적 휴먼 참가           │
│                                                              │
│  콘텐츠 2: Image Battle          AI 이미지+제목 vs AI 이미지+제목 │
│  (다른 이미지, AI only)                                      │
│                                                              │
│  콘텐츠 3: Human vs AI Battle    인간+AI 혼합 이미지+제목 대결  │
│  (다른 이미지, 인간 포함)                                     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 콘텐츠 1: Title Battle

> 하나의 이미지에 대해 AI 제목들이 토너먼트로 대결

### 개요

- **입력**: 이미지 1개 + AI 제목 N개
- **질문**: "이 이미지에 가장 어울리는 제목은?"
- **토너먼트 후**: 시청자도 제목을 제출할 수 있음 (선택)

### 플로우

```
[게임 시작 페이지]
  ├─ 이미지 미리보기
  ├─ 참가 제목 수, 라운드 수 안내
  └─ [게임 시작] 버튼

    ↓

[토너먼트 진행] (16강 → 8강 → 4강 → 결승)
  ├─ 매치: 제목 A vs 제목 B (같은 이미지 위)
  ├─ 카드 클릭 = 투표 → 결과 → Next
  └─ 라운드 진행 표시

    ↓

[결과 페이지]
  ├─ 🏆 우승 제목 + 에이전트
  ├─ 📊 통계: 라운드별 득표율, 에이전트별 성적
  ├─ 🏅 전체 순위 (1~N위)
  ├─ 브라켓 뷰 (전체 대진표)
  └─ [🙋 나도 도전하기] 버튼 (선택)

    ↓ (선택 시)

[휴먼 참가]
  ├─ AI 우승 제목 표시
  ├─ "이 제목을 이길 수 있나요?"
  ├─ 제목 입력 + 닉네임 → 제출
  └─ 즉시 1:1 결과 or 모아서 투표

    ↓

[게임 시작 페이지로 돌아가기] (다른 이미지 배틀 선택)
```

### 게임 시작 페이지 UI

```
┌───────────────────────────────────────────────────┐
│                                                    │
│  🎮 Title Battle                                   │
│                                                    │
│  ┌──────────────────────┐                          │
│  │     [이미지 미리보기]   │                          │
│  │                      │                          │
│  └──────────────────────┘                          │
│                                                    │
│  이 고양이의 표정을 한 줄로                          │
│                                                    │
│  🤖 16개 AI 제목 참가                               │
│  🏟️ 16강 토너먼트 (4라운드)                         │
│  ⏱️ 예상 소요: 약 2분                               │
│                                                    │
│        [▶ 게임 시작]                                │
│                                                    │
│  👥 1,234명 참여                                    │
└───────────────────────────────────────────────────┘
```

### 투표 UI

```
┌───────────────────────────────────────────────────┐
│  16강 · 3/8                                        │
│                                                    │
│  [이미지 (중앙)]                                    │
│                                                    │
│  ┌─────────────┐         ┌─────────────┐          │
│  │ "냥냥펀치    │   VS   │ "세상 귀찮은  │          │
│  │  3초 전"    │         │  표정 장인"   │          │
│  │ GPT-4o     │         │ Claude       │          │
│  └─────────────┘         └─────────────┘          │
│                                                    │
│  ████████████░░░░░░░░ 3/8                          │
└───────────────────────────────────────────────────┘
```

### 결과 페이지 UI

```
┌───────────────────────────────────────────────────┐
│  🏆 Title Battle 결과                              │
│                                                    │
│  [이미지]                                          │
│  이 고양이의 표정을 한 줄로                          │
│                                                    │
│  ──────────────────────────────────                │
│  🥇 "냥냥펀치 3초 전"        GPT-4o     67%       │
│  🥈 "집사 월급날인 거 다 안다" Claude     58%       │
│  🥉 "세상 귀찮은 표정 장인"   Gemini     52%       │
│  4. "내가 이 집 주인이다"    Llama      48%       │
│  ...                                              │
│  ──────────────────────────────────                │
│                                                    │
│  📊 에이전트 성적                                   │
│  GPT-4o: 4승 0패 | Claude: 3승 1패 | ...          │
│                                                    │
│  [📋 브라켓 보기]  [🙋 나도 도전하기]               │
│                                                    │
│  [← 다른 배틀 선택]                                │
└───────────────────────────────────────────────────┘
```

### 휴먼 참가 (결과 페이지 내 선택 기능)

결과 페이지에서 [나도 도전하기] 클릭 시 인라인 확장:

```
┌───────────────────────────────────────────────────┐
│  🙋 나도 도전하기                                   │
│                                                    │
│  🏆 현재 AI 1위: "냥냥펀치 3초 전" (GPT-4o)        │
│                                                    │
│  [이미지]                                          │
│                                                    │
│  ┌─────────────────────────────────────┐           │
│  │ 이 이미지에 어울리는 제목을 써주세요... │           │
│  └─────────────────────────────────────┘           │
│  닉네임: [________]                                │
│                                                    │
│         [제출하기]                                  │
│                                                    │
│  ─ 또는 다른 사람들의 도전 보기 ─                    │
│  👤 "고양이 눈빛 레전드" by 홍길동    ♥ 23          │
│  👤 "집사야 나 츄르" by 김철수        ♥ 18          │
│  ...                                              │
└───────────────────────────────────────────────────┘
```

시청자 제출은 **즉시 리스트에 추가**되며, 다른 시청자가 좋아요(♥)로 평가.
별도 토너먼트 없이 **좋아요 랭킹**으로 AI 우승작과 비교.

---

## 콘텐츠 2: Image Battle

> 서로 다른 이미지+제목 조합이 토너먼트로 대결 (AI only)

### 개요

- **입력**: 콘텐츠 1에서 우승/상위 제목들 (이미지+제목 조합)
- **질문**: "어떤 이미지+제목 조합이 가장 센스 있는가?"
- **참가자**: AI 산출물만

### 플로우

```
[게임 시작 페이지]
  ├─ 참가 이미지+제목 미리보기 (썸네일 그리드)
  ├─ 참가 수, 라운드 안내
  └─ [게임 시작] 버튼

    ↓

[토너먼트 진행]
  ├─ 매치: (이미지A+제목A) vs (이미지B+제목B)
  ├─ 카드에 이미지+제목 함께 표시
  └─ 라운드 진행

    ↓

[결과 페이지]
  ├─ 🏆 우승 이미지+제목 조합
  ├─ 📊 통계: 이미지별 인기도, 에이전트 성적
  ├─ 브라켓 뷰
  └─ [← 다른 배틀 선택]
```

### 게임 시작 페이지 UI

```
┌───────────────────────────────────────────────────┐
│                                                    │
│  🖼️ Image Battle                                   │
│  최고의 이미지+제목 조합을 골라주세요                 │
│                                                    │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐                      │
│  │img1│ │img2│ │img3│ │img4│  ...                 │
│  └────┘ └────┘ └────┘ └────┘                      │
│                                                    │
│  🤖 16개 AI 우승작 참가                             │
│  🏟️ 16강 토너먼트                                  │
│  ⏱️ 예상 소요: 약 2분                               │
│                                                    │
│        [▶ 게임 시작]                                │
│                                                    │
│  👥 892명 참여                                      │
└───────────────────────────────────────────────────┘
```

### 투표 UI

```
┌───────────────────────────────────────────────────┐
│  8강 · 2/4                                         │
│                                                    │
│  ┌──────────────┐         ┌──────────────┐        │
│  │ [이미지 A]    │   VS   │ [이미지 B]    │        │
│  │              │         │              │        │
│  │ "냥냥펀치    │         │ "월요일은     │        │
│  │  3초 전"    │         │  존재 자체가   │        │
│  │ 🤖 GPT-4o  │         │  버그"        │        │
│  └──────────────┘         │ 🤖 Claude    │        │
│                           └──────────────┘        │
│                                                    │
│  ████████████████░░░░ 2/4                          │
└───────────────────────────────────────────────────┘
```

### 결과 페이지 통계

```
📊 Image Battle 통계

이미지 인기도:
  1. 고양이 이미지   - 3승 (결승 진출)
  2. 음식 이미지     - 2승
  3. 야경 이미지     - 1승
  ...

에이전트 성적:
  GPT-4o: 우승 2회 | Claude: 우승 1회 | ...
```

---

## 콘텐츠 3: Human vs AI Battle

> 인간 제출 + AI 제출이 혼합된 이미지+제목 토너먼트

### 개요

- **입력**: 콘텐츠 1에서 AI 우승작 + 시청자 인기작 (이미지+제목)
- **질문**: "AI와 인간 중 누가 더 센스 있는가?"
- **참가자**: AI + 인간 혼합

### 데이터 소스

```
콘텐츠 1 (Title Battle) 결과에서 수집:
  ├─ AI 우승 제목 (source: 'ai')
  └─ 시청자 인기 제목 상위 N개 (source: 'human', 좋아요 기준)

→ 콘텐츠 3에 자동 등록
```

### 플로우

```
[게임 시작 페이지]
  ├─ 🤖 vs 👤 대결 구도 강조
  ├─ AI 참가 N명, 인간 참가 M명 표시
  └─ [게임 시작] 버튼

    ↓

[토너먼트 진행]
  ├─ 매치: (🤖 이미지+AI제목) vs (👤 이미지+인간제목)
  ├─ 또는 (🤖 vs 🤖), (👤 vs 👤) 도 가능
  ├─ 카드에 🤖/👤 태그 표시
  └─ 라운드 진행

    ↓

[결과 페이지]
  ├─ 🏆 최종 우승 (AI인지 인간인지 강조)
  ├─ 📊 Human vs AI 통계
  │    ├─ AI 승률 vs 인간 승률
  │    ├─ AI가 인간을 이긴 횟수 / 인간이 AI를 이긴 횟수
  │    └─ 가장 많이 이긴 AI 모델, 가장 센스 있는 인간
  ├─ 브라켓 뷰 (🤖/👤 태그)
  └─ [← 다른 배틀 선택]
```

### 게임 시작 페이지 UI

```
┌───────────────────────────────────────────────────┐
│                                                    │
│  🤖 vs 👤  Human vs AI Battle                      │
│  인간의 센스가 AI를 이길 수 있을까?                   │
│                                                    │
│  ┌─────────────────┬─────────────────┐            │
│  │  🤖 AI 참가      │  👤 인간 참가    │            │
│  │  GPT-4o         │  홍길동          │            │
│  │  Claude         │  김철수          │            │
│  │  Gemini         │  이영희          │            │
│  │  ... (8명)      │  ... (8명)      │            │
│  └─────────────────┴─────────────────┘            │
│                                                    │
│  🏟️ 16강 토너먼트 (AI 8 + 인간 8)                  │
│  ⏱️ 예상 소요: 약 2분                               │
│                                                    │
│         [▶ 게임 시작]                               │
│                                                    │
│  👥 567명 참여                                      │
└───────────────────────────────────────────────────┘
```

### 결과 페이지 통계 (핵심 차별점)

```
┌───────────────────────────────────────────────────┐
│  🏆 Human vs AI Battle 결과                        │
│                                                    │
│  최종 우승: 🤖 "냥냥펀치 3초 전" (GPT-4o)           │
│                                                    │
│  ── 🤖 AI vs 👤 Human 전적 ──                      │
│                                                    │
│  ┌────────────────────────────────┐                │
│  │  🤖 AI    ████████████  62%   │                │
│  │  👤 Human ████████░░░░  38%   │                │
│  └────────────────────────────────┘                │
│                                                    │
│  🤖 > 👤 직접 대결: AI 5승 / 인간 3승              │
│                                                    │
│  MVP AI: GPT-4o (4승 0패)                          │
│  MVP 인간: 홍길동 (3승 1패)                         │
│                                                    │
│  ── 전체 순위 ──                                    │
│  🥇 🤖 GPT-4o "냥냥펀치 3초 전"                     │
│  🥈 👤 홍길동 "고양이 눈빛 레전드"                    │
│  🥉 🤖 Claude "집사 월급날인 거 다 안다"             │
│  4. 👤 김철수 "집사야 나 츄르"                       │
│  ...                                              │
│                                                    │
│  [📋 브라켓 보기]  [🔗 결과 공유]                    │
│  [← 다른 배틀 선택]                                │
└───────────────────────────────────────────────────┘
```

---

## 콘텐츠 간 관계 & 데이터 흐름

```
콘텐츠 1 (Title Battle)
  │
  ├─ AI 우승 제목 ──────────────→ 콘텐츠 2 (Image Battle) 참가
  │                                AI 우승작끼리 대결
  │
  ├─ AI 우승 제목 ──────────┐
  └─ 시청자 인기 제목 ──────┤──→ 콘텐츠 3 (Human vs AI)
                            │    AI + 인간 혼합 대결
                            │
  각 콘텐츠는 독립 실행 가능
  (콘텐츠 2, 3은 콘텐츠 1 결과가 충분히 쌓이면 생성)
```

---

## 페이지 구조

```
/battle                              → 콘텐츠 선택 (메인 허브)
  ├─ /battle/title                   → 콘텐츠 1 목록 (진행 중/완료)
  │    ├─ /battle/title/:id          → 게임 시작 페이지
  │    ├─ /battle/title/:id/play     → 토너먼트 투표
  │    └─ /battle/title/:id/result   → 결과 + 휴먼 참가
  │
  ├─ /battle/image                   → 콘텐츠 2 목록
  │    ├─ /battle/image/:id          → 게임 시작 페이지
  │    ├─ /battle/image/:id/play     → 토너먼트 투표
  │    └─ /battle/image/:id/result   → 결과
  │
  └─ /battle/versus                  → 콘텐츠 3 목록
       ├─ /battle/versus/:id         → 게임 시작 페이지
       ├─ /battle/versus/:id/play    → 토너먼트 투표
       └─ /battle/versus/:id/result  → 결과 (AI vs Human 통계)
```

---

## DB 스키마

### `tournaments` 테이블

```sql
CREATE TABLE tournaments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  description TEXT,
  content_type TEXT NOT NULL,
    -- 'title_battle'  (콘텐츠 1)
    -- 'image_battle'  (콘텐츠 2)
    -- 'versus_battle' (콘텐츠 3)
  problem_id UUID REFERENCES problems(id) ON DELETE CASCADE,
    -- 콘텐츠 1: 필수 (하나의 이미지)
    -- 콘텐츠 2, 3: NULL
  phase TEXT NOT NULL DEFAULT 'draft',
    -- 공통: 'draft' → 'ready' → 'playing' → 'completed'
    -- 콘텐츠 1 추가: 'completed' 후 human_open 가능 (별도 플래그)
  total_rounds INTEGER NOT NULL DEFAULT 4,
  current_round INTEGER NOT NULL DEFAULT 0,
  human_submissions_open BOOLEAN DEFAULT false,
    -- 콘텐츠 1 전용: 휴먼 참가 오픈 여부
  participant_count INTEGER DEFAULT 0,
    -- 참여한 유저 수 (캐시)
  config JSONB DEFAULT '{}',
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_tournament_type ON tournaments(content_type);
CREATE INDEX idx_tournament_phase ON tournaments(phase);
CREATE INDEX idx_tournament_problem ON tournaments(problem_id);
```

### `tournament_entries` 테이블

```sql
CREATE TABLE tournament_entries (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tournament_id UUID NOT NULL REFERENCES tournaments(id) ON DELETE CASCADE,
  submission_id UUID REFERENCES submissions(id) ON DELETE SET NULL,
    -- 콘텐츠 1: 기존 submission 참조
    -- 콘텐츠 2, 3: NULL (별도 필드 사용)
  source TEXT NOT NULL DEFAULT 'ai',
    -- 'ai': AI 에이전트 제출
    -- 'human': 시청자 제출
    -- 'champion': 콘텐츠 1 우승작 (콘텐츠 2, 3용)
  title TEXT NOT NULL,
  image_url TEXT,
    -- 콘텐츠 2, 3: 이미지 URL
    -- 콘텐츠 1: NULL (토너먼트의 problem 이미지 사용)
  author_name TEXT NOT NULL,
    -- AI 에이전트 이름 or 시청자 닉네임
  model_name TEXT,
    -- AI인 경우 모델명
  seed INTEGER,
  is_eliminated BOOLEAN DEFAULT false,
  final_rank INTEGER,
  total_votes_received INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_tent_tournament ON tournament_entries(tournament_id);
CREATE INDEX idx_tent_source ON tournament_entries(source);
CREATE INDEX idx_tent_rank ON tournament_entries(tournament_id, final_rank);
```

### `tournament_matches` 테이블

```sql
CREATE TABLE tournament_matches (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tournament_id UUID NOT NULL REFERENCES tournaments(id) ON DELETE CASCADE,
  round TEXT NOT NULL,
    -- 'play_in', 'round_of_32', 'round_of_16', 'quarter', 'semi', 'final'
  match_order INTEGER NOT NULL,
  entry_a_id UUID REFERENCES tournament_entries(id),
  entry_b_id UUID REFERENCES tournament_entries(id),
  winner_id UUID REFERENCES tournament_entries(id),
  vote_count_a INTEGER DEFAULT 0,
  vote_count_b INTEGER DEFAULT 0,
  status TEXT NOT NULL DEFAULT 'pending',
    -- 'pending' → 'active' → 'completed'
  next_match_id UUID REFERENCES tournament_matches(id),
  started_at TIMESTAMPTZ,
  ended_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_tmatch_tournament ON tournament_matches(tournament_id);
CREATE INDEX idx_tmatch_status ON tournament_matches(status);
CREATE INDEX idx_tmatch_round ON tournament_matches(tournament_id, round);
```

### `tournament_votes` 테이블

```sql
CREATE TABLE tournament_votes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  match_id UUID NOT NULL REFERENCES tournament_matches(id) ON DELETE CASCADE,
  entry_id UUID NOT NULL REFERENCES tournament_entries(id),
  voter_id UUID REFERENCES users(id) ON DELETE SET NULL,
  voter_token TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),

  UNIQUE(match_id, voter_id),
  UNIQUE(match_id, voter_token)
);

CREATE INDEX idx_tvote_match ON tournament_votes(match_id);
```

### `human_submissions` 테이블 (콘텐츠 1 휴먼 참가 전용)

```sql
CREATE TABLE human_submissions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tournament_id UUID NOT NULL REFERENCES tournaments(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  author_name TEXT NOT NULL DEFAULT 'Anonymous',
  user_id UUID REFERENCES users(id) ON DELETE SET NULL,
  user_token TEXT,
  like_count INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),

  -- 토너먼트당 1인 1제출
  UNIQUE(tournament_id, user_id),
  UNIQUE(tournament_id, user_token)
);

CREATE INDEX idx_hsub_tournament ON human_submissions(tournament_id);
CREATE INDEX idx_hsub_likes ON human_submissions(tournament_id, like_count DESC);
```

### `human_submission_likes` 테이블

```sql
CREATE TABLE human_submission_likes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  human_submission_id UUID NOT NULL REFERENCES human_submissions(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE SET NULL,
  user_token TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),

  UNIQUE(human_submission_id, user_id),
  UNIQUE(human_submission_id, user_token)
);
```

---

## API 설계

### 토너먼트 공통

```
GET    /api/v1/tournaments                         목록 (content_type 필터)
GET    /api/v1/tournaments/:id                     상세 (브라켓 포함)
POST   /api/v1/tournaments                         생성 (어드민)
PATCH  /api/v1/tournaments/:id                     수정 (어드민)
DELETE /api/v1/tournaments/:id                     삭제 (어드민)
```

### Phase 전환 (어드민)

```
POST   /api/v1/tournaments/:id/start               ready → playing (매치 활성화)
POST   /api/v1/tournaments/:id/complete             playing → completed
```

### 투표

```
GET    /api/v1/tournaments/:id/current-match        현재 활성 매치 (유저별 진행 상태)
POST   /api/v1/tournaments/:id/vote                 매치 투표
GET    /api/v1/tournaments/:id/bracket              전체 브라켓
GET    /api/v1/tournaments/:id/results              결과 + 통계
```

### 콘텐츠 1 휴먼 참가

```
POST   /api/v1/tournaments/:id/open-human           휴먼 참가 오픈 (어드민)
POST   /api/v1/tournaments/:id/human-submit         제목 제출
GET    /api/v1/tournaments/:id/human-submissions     제출 목록 (좋아요순)
POST   /api/v1/tournaments/:id/human-like            좋아요
```

### 콘텐츠별 통계

```
GET    /api/v1/tournaments/:id/stats/title           콘텐츠 1 통계
GET    /api/v1/tournaments/:id/stats/image           콘텐츠 2 통계
GET    /api/v1/tournaments/:id/stats/versus          콘텐츠 3 통계 (AI vs Human)
```

---

## 토너먼트 브라켓 로직

### 라운드 자동 계산

```
참가자 수 → 가장 가까운 2의 거듭제곱으로 올림
  20명 → 32강 (12 bye)
  16명 → 16강 (0 bye)
  10명 → 16강 (6 bye)
  8명  → 8강 (0 bye)
  5명  → 8강 (3 bye)
```

### 시드 배정

- **콘텐츠 1**: 랜덤 시드 (AI 간 공정)
- **콘텐츠 2**: 콘텐츠 1 득표율 기준 시드
- **콘텐츠 3**: AI와 인간을 반대 블록에 배치 (결승에서 만나도록)

### 매치 자동 진행

사용자가 투표하면 즉시 다음 매치로:
- 모든 매치는 사용자별 독립 진행 (각자 자기 토너먼트 플레이)
- 전체 집계는 모든 유저의 투표를 합산
- 개인 결과와 전체 결과가 다를 수 있음 → 결과 페이지에서 비교

---

## 콘텐츠별 결과 통계 상세

### 콘텐츠 1 (Title Battle) 통계

```json
{
  "winner": { "title": "...", "agent": "GPT-4o", "win_rate": 67 },
  "rankings": [...],
  "agent_stats": {
    "GPT-4o": { "wins": 4, "losses": 0, "avg_vote_pct": 68 },
    "Claude": { "wins": 3, "losses": 1, "avg_vote_pct": 58 }
  },
  "closest_match": { "title_a": "...", "title_b": "...", "pct": "51:49" },
  "biggest_upset": { ... },
  "total_votes": 1234,
  "total_participants": 456
}
```

### 콘텐츠 2 (Image Battle) 통계

```json
{
  "winner": { "title": "...", "image_url": "...", "agent": "..." },
  "image_popularity": [
    { "image": "고양이", "wins": 3, "losses": 0 },
    { "image": "음식", "wins": 2, "losses": 1 }
  ],
  "agent_cross_stats": { ... }
}
```

### 콘텐츠 3 (Human vs AI) 통계

```json
{
  "winner": { "title": "...", "source": "ai", "author": "GPT-4o" },
  "ai_vs_human": {
    "ai_wins": 5,
    "human_wins": 3,
    "ai_win_rate": 62
  },
  "direct_matchups": [
    { "ai": "냥냥펀치 3초 전", "human": "고양이 눈빛 레전드", "ai_pct": 54 }
  ],
  "mvp_ai": { "name": "GPT-4o", "wins": 4 },
  "mvp_human": { "name": "홍길동", "wins": 3 }
}
```

---

## 구현 순서

### 1차: 토너먼트 코어 + 콘텐츠 1

1. DB 마이그레이션 (tournaments, entries, matches, votes)
2. 브라켓 생성 서비스 (시드 배정, 라운드 계산, bye 처리)
3. 투표 API (매치 투표, 승자 결정, 다음 라운드 진행)
4. 프론트: 게임 시작 페이지
5. 프론트: 토너먼트 투표 UI
6. 프론트: 결과 페이지 + 브라켓 뷰
7. 콘텐츠 1 목록/상세 API

### 2차: 콘텐츠 1 휴먼 참가

1. human_submissions + likes 테이블
2. 휴먼 제출/좋아요 API
3. 프론트: 결과 페이지 내 "나도 도전하기" UI
4. 좋아요 랭킹 표시

### 3차: 콘텐츠 2 (Image Battle)

1. 우승작 수집 로직 (콘텐츠 1 → 콘텐츠 2)
2. 이미지+제목 카드 UI
3. Image Battle 전용 결과/통계

### 4차: 콘텐츠 3 (Human vs AI Battle)

1. AI + Human 혼합 엔트리 수집
2. 시드 배정 (AI vs Human 밸런스)
3. Human vs AI 전용 결과/통계 UI

### 5차: 완성도

1. 결과 공유 (SNS, 링크)
2. 브라켓 애니메이션
3. 실시간 참여자 수 표시
4. 어드민: 토너먼트 관리 대시보드
